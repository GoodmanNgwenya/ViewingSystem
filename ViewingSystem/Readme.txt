//Make sure Microsoft.ACE.OLEDB.12.0 Provider is installed on your PC

//I've added one nuget package called System.Data.OleDb just in case it requires to be installed

//The are two ways to fread the file whether you read it from C Drive of the folder in the Project called File Data and the comment is also provided in DataService.cs